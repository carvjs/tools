{
  "name": "@carv/snowpack-template-form",
  "version": "1.0.0",
  "private": true,
  "keywords": [
    "csa-template"
  ],
  "license": "MIT",
  "source": "src/form.svelte",
  "scripts": {
    "build": "snowpack build",
    "fix": "prettier --ignore-path .gitignore --write . && eslint --ignore-path .gitignore --fix .",
    "lint": "eslint --ignore-path .gitignore . && prettier --ignore-path .gitignore --check .",
    "start": "snowpack dev",
    "pretest": "npm run lint",
    "test": "jest",
    "test:coverage": "npm test -- --coverage",
    "test:watch": "npm test -- --watch"
  },
  "prettier": "@carv/prettier-config",
  "eslintConfig": {
    "extends": "@carv/eslint-config",
    "root": true
  },
  "jest": {
    "preset": "@carv/snowpack-scripts-svelte"
  },
  "devDependencies": {
    "@carv/snowpack-scripts-svelte": "^1.0.0",
    "eslint": "^7.2.0",
    "jest": "^26.1.0",
    "prettier": "^2.0.5",
    "snowpack": "^2.5.0",
    "svelte": "^3.21.0",
    "svelte-formup": "^0.0.2",
    "svelte-htm": "^1.0.2",
    "yup": "^0.29.1"
  },
  "peerDependencies": {
    "svelte": "^3.21.0",
    "svelte-formup": "^0.0.2",
    "yup": "^0.29.1"
  },
  "snowpack": {
    "extends": "@carv/snowpack-scripts-svelte"
  }
}
